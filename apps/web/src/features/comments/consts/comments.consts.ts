/* Border classes derived from PLATFORM_IDENTITY_COLORS (src/lib/platform.consts.ts) —
   kept as class literals so Tailwind can statically extract them. */
export const PLATFORM_COLORS: Record<string, string> = {
  tiktok: 'border-l-[#25F4EE]',
  facebook: 'border-l-[#1877F2]',
};
