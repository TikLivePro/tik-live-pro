# ==============================================================================
# Live Session Service — Deployment + Service + HPA
# Port: 3003
# ==============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: live-session-service
  namespace: tik-live-pro
  labels:
    app: live-session-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: live-session-service
  template:
    metadata:
      labels:
        app: live-session-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "3003"
    spec:
      containers:
        - name: live-session-service
          image: ghcr.io/tik-live-pro/live-session-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3003
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3003"
            - name: LOG_LEVEL
              value: "info"
            - name: NATS_URL
              value: nats://nats-service:4222
            - name: CORRELATION_ID_HEADER
              value: x-correlation-id
            - name: TRACE_ID_HEADER
              value: x-trace-id
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: live-session-secrets
                  key: database-url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: live-session-secrets
                  key: jwt-secret
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 3003
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 3

---
apiVersion: v1
kind: Service
metadata:
  name: live-session-service
  namespace: tik-live-pro
  labels:
    app: live-session-service
spec:
  selector:
    app: live-session-service
  ports:
    - port: 3003
      targetPort: 3003
      name: http

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: live-session-service-hpa
  namespace: tik-live-pro
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: live-session-service
  minReplicas: 2
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 75
