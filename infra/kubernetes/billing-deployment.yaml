# ==============================================================================
# Billing Service — Deployment + Service + HPA
# Port: 3004
# ==============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-service
  namespace: tik-live-pro
  labels:
    app: billing-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: billing-service
  template:
    metadata:
      labels:
        app: billing-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "3004"
    spec:
      containers:
        - name: billing-service
          image: ghcr.io/tik-live-pro/billing-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3004
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3004"
            - name: LOG_LEVEL
              value: "info"
            - name: NATS_URL
              value: nats://nats-service:4222
            - name: CORRELATION_ID_HEADER
              value: x-correlation-id
            - name: TRACE_ID_HEADER
              value: x-trace-id
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: billing-service-secrets
                  key: database-url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: billing-service-secrets
                  key: jwt-secret
            - name: STRIPE_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: billing-service-secrets
                  key: stripe-secret-key
            - name: STRIPE_WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: billing-service-secrets
                  key: stripe-webhook-secret
            - name: STRIPE_PREMIUM_PRICE_ID
              valueFrom:
                secretKeyRef:
                  name: billing-service-secrets
                  key: stripe-premium-price-id
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 3004
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 3

---
apiVersion: v1
kind: Service
metadata:
  name: billing-service
  namespace: tik-live-pro
  labels:
    app: billing-service
spec:
  selector:
    app: billing-service
  ports:
    - port: 3004
      targetPort: 3004
      name: http

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: billing-service-hpa
  namespace: tik-live-pro
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: billing-service
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
