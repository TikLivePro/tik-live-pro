# ==============================================================================
# Comments Service — Deployment + Service + HPA
# Port: 3006
# ==============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: comments-service
  namespace: tik-live-pro
  labels:
    app: comments-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: comments-service
  template:
    metadata:
      labels:
        app: comments-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "3006"
    spec:
      containers:
        - name: comments-service
          image: ghcr.io/tik-live-pro/comments-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3006
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3006"
            - name: LOG_LEVEL
              value: "info"
            - name: NATS_URL
              value: nats://nats-service:4222
            # Poll interval in ms — reduce in production for lower latency
            - name: COMMENT_POLL_INTERVAL_MS
              value: "2000"
            - name: CORRELATION_ID_HEADER
              value: x-correlation-id
            - name: TRACE_ID_HEADER
              value: x-trace-id
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: comments-service-secrets
                  key: database-url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: comments-service-secrets
                  key: jwt-secret
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3006
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 3006
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 3

---
apiVersion: v1
kind: Service
metadata:
  name: comments-service
  namespace: tik-live-pro
  labels:
    app: comments-service
spec:
  selector:
    app: comments-service
  ports:
    - port: 3006
      targetPort: 3006
      name: http

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: comments-service-hpa
  namespace: tik-live-pro
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: comments-service
  minReplicas: 2
  maxReplicas: 15
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 65
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 75
