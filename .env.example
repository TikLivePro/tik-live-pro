# Base Postgres URL used by scripts/db-migrate.sh and scripts/db-generate.sh.
# Each service's database name is appended automatically (e.g. .../tiklive_auth).
# Override this when your Postgres host, port, or credentials differ from the defaults.
DB_BASE_URL=postgresql://postgres:password@localhost:5432
